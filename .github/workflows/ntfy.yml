name: ntfy
on: [push]
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: niniyas/ntfy-action@master
        with:
          url: 'https://ntfy.sh'
          topic: 'test'
          details: 'hello world'
